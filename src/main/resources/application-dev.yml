spring:
  application:
    name: Omni-BE-Payment

cors:
  urls:
    httpsGatewayURL: ${HTTPS_GATEWAY_URL}

springdoc:
  api-docs:
    path: ${PAYMENT_SWAGGER_PATH}
  swagger-ui:
    path: ${PAYMENT_SWAGGER_PATH}