spring:
  application:
    name: Omni-BE-Payment
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

cors:
  urls:
    httpsGatewayURL: ${HTTPS_GATEWAY_URL}

springdoc:
  api-docs:
    path: ${PAYMENT_SWAGGER_PATH}
  swagger-ui:
    path: ${PAYMENT_SWAGGER_PATH}

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, metrics
  endpoint:
    health:
      show-details: never
  metrics:
    export:
      prometheus:
        enabled: true
    distribution:
      percentiles:
        http.server.requests: 0.5, 0.95, 0.99
      percentiles-histogram:
        http.server.requests: true

CARD_SERVER_ADDRESS: ${CARD_SERVER_ADDRESS}
SPONSOR_SERVER_ADDRESS: ${SPONSOR_SERVER_ADDRESS}
AI_SERVER_ADDRESS: ${AI_SERVER_ADDRESS}
USER_SERVER_ADDRESS: ${USER_SERVER_ADDRESS}

toss:
  widgetSecretKey: ${TOSS_WIDGET_SECRET_KEY}
  url: ${TOSS_SERVER_ADDRESS}